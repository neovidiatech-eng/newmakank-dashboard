import { UpdatePasswordForm } from "@/components/pages/(auth)/update-password/update-password-form";

export default function Page(): JSX.Element {
  return (
    <div className="flex h-screen w-full items-center justify-center px-4">
      <UpdatePasswordForm />
    </div>
  );
}
